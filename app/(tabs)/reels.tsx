import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { ThemedText } from '@/components/ThemedText'

const reels = () => {
  return (
    <View>
      <ThemedText>reels</ThemedText>
    </View>
  )
}

export default reels

const styles = StyleSheet.create({})